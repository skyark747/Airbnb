import { createContext } from "react";

const StayDateIn = createContext(null);
const StayDateOut = createContext(null);
const StayGuests = createContext(null);

export default { StayDateIn, StayDateOut, StayGuests };